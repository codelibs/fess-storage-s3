<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE components PUBLIC "-//DBFLUTE//DTD LastaDi 1.0//EN"
	"http://dbflute.org/meta/lastadi10.dtd">
<components>
	<!-- Merged into Fess's fess_storage.xml. StorageClientFactory looks the client up as
	     "<storage.type>StorageClient", so this component name is what makes storage.type=s3 work.
	     A "++" file is additive and is collected from every jar on the class path, so this does not
	     collide with another fess-storage-* plugin contributing its own client.

	     Prototype, not singleton: callers take the client in a try-with-resources and close it. -->
	<component name="s3StorageClient" class="org.codelibs.fess.storage.s3.S3StorageClient" instance="prototype">
	</component>
	<!-- S3_COMPAT is the auto-detection result for MinIO and the like, and it is served by the
	     same client as s3. It is a separate component rather than a branch in Java because that
	     keeps the type-to-class mapping in one place. -->
	<component name="s3_compatStorageClient" class="org.codelibs.fess.storage.s3.S3StorageClient" instance="prototype">
	</component>
</components>
